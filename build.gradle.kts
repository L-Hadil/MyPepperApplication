// Top-level build file where you can add configuration options common to all sub-projects/modules.
plugins {
    // Versions stables et disponibles
    id("com.android.application") version "8.3.1" apply false
    id("org.jetbrains.kotlin.android") version "1.8.22" apply false
    // Ajoutez d'autres plugins top-level si besoin
}

// Ici, on peut éventuellement ajouter des configurations globales si nécessaire,
// mais la plupart des réglages se font dans le module `app`.
